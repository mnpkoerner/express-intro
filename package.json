{
  "name": "express-intro",
  "version": "1.0.0",
  "description": "In gitignore, we tell git what not to track: node_modules/ all the librarys",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
